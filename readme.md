# Features

## Done
- Battlemap option per scene. If present, the DM screen's **Combat** toggle displays the battlemap (tagged "Map"); scenes without one keep their scene image.
- Display monster and battlemap (combat view shows only enemies and tactics).
- Toggle Combat / Non-Combat for each scene (free toggle, keeps the images).
- Battlemap library: `library/battlemaps/data.js` (generated by `scripts/build-battlemaps.js`) maps physical-book maps to their files; scenes reference library ids like `'ttb1-p31-l-underground'` and the DM screen shows a "find the book" chip with book, page, side, and file path.

## Ideas
- Initiative tracker with order of characters. (Can add characters as data in data file in sessions, with master data in characters folder.)

# Features for proper server with databases etc.
- Is it possible to edit the encounter and save the file? when running locally? Dont think so.
- View all monsters, adding monsters on the fly.

# Agents
Should I have a separate agent for this? Or is skill enough?